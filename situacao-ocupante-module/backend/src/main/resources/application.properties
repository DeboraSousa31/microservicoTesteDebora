quarkus.http.port=8081

quarkus.datasource.db-kind=postgresql
quarkus.datasource.reactive.url=postgresql://localhost:5432/bancotestedouglas
quarkus.datasource.username=postgres
quarkus.datasource.password=@#fxil@#25

quarkus.hibernate-orm.database.generation=none
quarkus.hibernate-orm.log.sql=false

quarkus.hibernate-orm.packages=br.com.foxinline.domain.model

# Desativa todos os DevServices que tentam subir containers
quarkus.devservices.enabled=false

quarkus.hibernate-orm.log.sql=true
quarkus.hibernate-orm.log.format-sql=true

# Auditoria
auditoria-api/mp-rest/url=http://localhost:9191
auditoria-api/mp-rest/scope=javax.inject.Singleton
auditoria-api/mp-rest/connectTimeout=5000
auditoria-api/mp-rest/readTimeout=10000

# Permite teste com o vue-teste
quarkus.http.cors=true
quarkus.http.cors.origins=http://localhost:5173, http://localhost:4173, http://localhost:4174, https://cerurb-vue.foxinline.com/
quarkus.http.cors.headers=accept, authorization, content-type, x-requested-with
quarkus.http.cors.methods=GET, POST, PUT, DELETE, OPTIONS


# # --- Configuração do Keycloak OIDC ---
# quarkus.oidc.auth-server-url=http://localhost:8080/realms/cerurb-realm
# quarkus.oidc.client-id=cerurb-vue-client

# # Tipo de aplicação (service para backend, web-app para app com login no server)
# quarkus.oidc.application-type=service

# # Exige que TODAS as rotas sejam autenticadas por padrão
# quarkus.http.auth.permission.default.paths=/*
# quarkus.http.auth.permission.default.policy=authenticated